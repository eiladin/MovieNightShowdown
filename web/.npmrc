# Pin this project to the public registry. Without it, `npm install` inherits
# whatever registry the developer's ~/.npmrc sets, which rewrites the `resolved`
# URLs in package-lock.json to a private host. CI and the Docker build have no
# credentials for such a host, so `npm ci` fails with a 401 for everyone else.
registry=https://registry.npmjs.org/
